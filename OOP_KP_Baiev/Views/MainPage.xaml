<Page x:Class="OOP_KP_Baiev.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="MainPage"
      Background="Transparent">
    <Page.Resources>
        <Style x:Key="MainPageButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinWidth" Value="250"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="DarkGray"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Border Width="Auto" MinWidth="350" MaxWidth="450" Background="White"
                CornerRadius="15" Padding="40" VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="1" Opacity="0.2" Color="Gray"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Головна сторінка"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"
                           Foreground="#FF1F2937"/>
                <Button Content="Мої проєкти"
                        Click="GoToMyProjects_Click"
                        Background="#FF4A90E2"  Style="{StaticResource MainPageButtonStyle}"/>
                <Button Content="Список фрілансерів"
                        Click="GoToFreelancers_Click"
                         Background="#FF50E3C2" Style="{StaticResource MainPageButtonStyle}"/>
                <Button Content="Список проєктів"
                        Click="GoToProjects_Click"
                         Background="#FF7ED321" Style="{StaticResource MainPageButtonStyle}"/>
                <Button Content="Мій профіль"
                        Click="GoToAccount_Click"
                         Background="#FFF5A623" Style="{StaticResource MainPageButtonStyle}"/>
                <Button x:Name="AdminPanelButton"
                        Content="Адмін панель"
                        Click="GoToAdminPanel_Click"
                        Background="#FFBD10E0" Style="{StaticResource MainPageButtonStyle}"
                        Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>